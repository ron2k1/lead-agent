{
  "id": "C-09",
  "description": "Cloudflare d1_database_query denied; arbitrary-SQL tools must never reach the lead even when the surrounding plugin has read tools enabled elsewhere.",
  "env": {},
  "event": {
    "tool_name": "mcp__plugin_cloudflare_cloudflare-bindings__d1_database_query",
    "tool_input": { "database": "x", "sql": "SELECT 1" }
  },
  "expected": {
    "exitCode": 2,
    "stdoutContains": "block"
  }
}
